#include <stdio.h>

// Function to compute GCD using Euclidean algorithm
long long gcd(long long a, long long b) {
    while (b != 0) {
        long long temp = b;
        b = a % b;
        a = temp;
    }
    return a;
}

// Function to compute LCM using GCD
long long lcm(long long n, long long m) {
    return (n / gcd(n, m)) * m;  // To prevent overflow: divide first, then multiply
}

int main() {
    long long N, M;
    scanf("%lld %lld", &N, &M);

    printf("%lld\n", lcm(N, M));

    return 0;
}
